const today = new Date();
today.getMonth();

// defining the class with as many properties as we want
class Person {
  age: 20;
}
// creating a new instance of the class
const Francisco = new Person();

// accessing the property of the new instance
Francisco.age;
